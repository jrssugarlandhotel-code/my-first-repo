services:
  - type: web
    name: hotel-sales-app
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        sync: false   # you'll paste your Neon connection string in during setup
